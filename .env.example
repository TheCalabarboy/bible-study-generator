# ===========================================
# SermonDive Environment Variables
# ===========================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to version control!

# -------------------------------------------
# SERVER-SIDE ONLY (Vercel Environment Variables)
# -------------------------------------------
# These are set in Vercel Dashboard > Settings > Environment Variables
# They are NOT prefixed with VITE_ so they stay server-side only

# Your Google Gemini API Key (required)
# Get one at: https://ai.google.dev/
GEMINI_API_KEY=your_gemini_api_key_here

# Gemini model to use (optional, defaults to gemini-2.5-pro)
GEMINI_MODEL=gemini-2.5-pro

# -------------------------------------------
# CLIENT-SIDE (Optional)
# -------------------------------------------
# These are prefixed with VITE_ and bundled into the frontend
# Only use for non-sensitive configuration

# Optional: Video ID for intro/demo video on homepage
VITE_INTRO_VIDEO_ID=your_intro_video_id

# Optional: API URL for local development (leave empty for production)
# VITE_API_URL=http://localhost:3000
